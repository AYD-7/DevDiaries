export const test = (req, res) => {
    res.json({message: "You have successfully tested the API."});
}

export const run = (req, res) => {
    res.json({message: "You have successfully run the API."});
}